<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="odoo-selection-enum.list">
      <field name="name">odoo-selection-enum list</field>
      <field name="model">odoo-selection-enum.odoo-selection-enum</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="odoo-selection-enum.action_window">
      <field name="name">odoo-selection-enum window</field>
      <field name="res_model">odoo-selection-enum.odoo-selection-enum</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="odoo-selection-enum.action_server">
      <field name="name">odoo-selection-enum server</field>
      <field name="model_id" ref="model_odoo-selection-enum_odoo-selection-enum"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="odoo-selection-enum" id="odoo-selection-enum.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="odoo-selection-enum.menu_1" parent="odoo-selection-enum.menu_root"/>
    <menuitem name="Menu 2" id="odoo-selection-enum.menu_2" parent="odoo-selection-enum.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="odoo-selection-enum.menu_1_list" parent="odoo-selection-enum.menu_1"
              action="odoo-selection-enum.action_window"/>
    <menuitem name="Server to list" id="odoo-selection-enum" parent="odoo-selection-enum.menu_2"
              action="odoo-selection-enum.action_server"/>
    -->
  </data>
</odoo>